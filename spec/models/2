require 'spec_helper'

describe Subscriber do

 context "import" do
   
   let(:sub_user) { Factory(:subscriber) }

   it "should not push users with duplicate email to database" do
      p "inside test case...."
      #p sub_user

   end
 end


end
